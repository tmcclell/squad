# Orchestration: Edie — Issue #250 squad migrate command

**Spawn Time:** 2026-03-07T17:35:45Z  
**Agent:** Edie (claude-sonnet-4.5)  
**Mode:** background  
**Issue:** #250  
**Commit:** 412ce58 (dev)

## Scope

Implement `squad migrate` command to support bidirectional migration between markdown-only and SDK-First modes, plus legacy `.ai-team/` upgrade.

## Work Done

### Created Files

- **`packages/squad-cli/src/cli/commands/migrate.ts`** (~600 lines)
  - Full implementation with parser, generator, and three migration paths
  - Type-safe output (no `any` types)

### Modified Files

- **`packages/squad-cli/src/cli-entry.ts`** (~240)
  - Wired migrate command after upgrade block
  - Help text added (~107)

### Migration Paths

1. **`squad migrate --to sdk`** (markdown → SDK-First)
   - Parses `team.md`, `routing.md`, `casting/policy.json`, agent charters
   - Generates `squad.config.ts` with builder functions
   - Type-safe with string escaping for all fields

2. **`squad migrate --to markdown`** (SDK-First → markdown)
   - Runs `squad build` to regenerate `.squad/` from config
   - Moves `squad.config.ts` → `squad.config.ts.bak`
   - Markdown becomes source of truth

3. **`squad migrate --from ai-team`** (legacy upgrade)
   - Uses existing `migrateDirectory()` function
   - Converts `.ai-team/` → `.squad/`
   - Suggests follow-up `squad migrate --to sdk`

4. **Interactive mode** (no flags)
   - Auto-detects current mode
   - Suggests appropriate migration path

### Features

- ✅ Dry-run support (`--dry-run` prints output without writing)
- ✅ Type safety (zero `any` types, all strings escaped)
- ✅ Round-trip fidelity (migrate → build produces identical `.squad/`)
- ✅ Comprehensive parsing (all metadata preserved)

## Validation

✅ Build clean (0 errors)  
✅ All tests pass (migrate.test.ts: 20+ test cases)  
✅ Interactive mode correctly detects SDK-First  
✅ Dry-run generates valid TypeScript  
✅ Parser handles multiline project context  
✅ String escaping tested for single quotes  

## Cross-Agent Dependencies

- **Fenster:** Related to #249 (init --sdk). Init is for new squads, migrate is for existing.
- **Hockney:** Test coverage in migrate.test.ts
- **Verbal:** Migration preserves orchestration instructions

## Status

✅ **Complete.** Ready for merge.
