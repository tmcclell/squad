# Orchestration Log: Fenster (agent-31)

**Task:** Fix #223: Model config reliability  
**Mode:** background  
**Model:** claude-sonnet-4.5  
**Timestamp:** 2026-03-07T14:16:01Z  

## Outcome

✅ **SUCCESS**

- Root cause identified: build pipeline emitted flat `**Model:** value` but charter-compiler parsed `## Model` + `**Preferred:** value`, causing silent loss of model preferences
- Fix: `AgentDefinition.model` now accepts `string | ModelPreference` (with `preferred`, `rationale`, `fallback` fields)
- Charter generation updated to emit proper `## Model` section
- New `defaults.model` on `SquadSDKConfig` for squad-level model defaults
- All 134 tests passing
- Backwards compatible
- PR #245 opened targeting `dev` branch, closes #223

## Files Modified

- `packages/squad-sdk/src/builders/types.ts`
- Charter generation pipeline

## Decisions

**Decision:** Model field type change  
**What:** `AgentDefinition.model` accepts `string | ModelPreference` instead of just `string`  
**Why:** Enables granular model preferences (preferred, fallback, rationale) alongside squad-level defaults. Prevents silent loss of model configuration during build.
**Impact:** Backwards compatible. Existing code using string values continues to work.
