# Orchestration Log: PR #233 CI Failure Investigation & Fix

**Date:** 2026-03-07T04:55:00Z  
**Agent:** Fenster (Core Dev)  
**Mode:** background

## Request Summary

Investigate and fix PR #233 CI failure. PR passed all 74 tests locally but failed 4 tests in CI.

## Investigation

### Failing Tests

- `test/cli-p0-regressions.test.ts` — bare semver test (expected 1 line, got 3)
- `test/speed-gates.test.ts` — version outputs one line (expected 1, got 3)
- `test/ux-gates.test.ts` — no overflow beyond 80 chars (ExperimentalWarning line >80)
- `test/ux-gates.test.ts` — version bare semver (expected 1 line, got 3)

### Root Cause

`node:sqlite` import triggers Node.js `ExperimentalWarning` that leaks to stderr. The existing `process.env.NODE_NO_WARNINGS = '1'` in cli-entry.ts was ineffective because Node only reads that env var at process startup, not when set at runtime.

## Solution

Added `process.emit` override in cli-entry.ts that intercepts `warning` events with `name === 'ExperimentalWarning'` and swallows them. Placed:
- After `process.env.NODE_NO_WARNINGS = '1'` (which still helps child processes)
- Before the `await import('node:sqlite')` pre-flight check

This is the standard Node.js pattern for runtime warning suppression.

## Implementation

- **File:** cli-entry.ts
- **Lines added:** 12 (comment + override function)
- **Change:** Process emit hook to swallow ExperimentalWarning events

## Outcomes

- ✅ All 4 previously failing tests now pass
- ✅ No regressions in structural tests (#624)
- ✅ ExperimentalWarning messages no longer appear in CLI output
- ✅ Other warnings (DeprecationWarning, etc.) remain unaffected
- ✅ CI green (3652 passing, 0 failing)
- ✅ Ready for merge

## Technical Details

**Pattern:** Standard Node.js runtime warning suppression via `process.emit` override. This approach is necessary because `NODE_NO_WARNINGS` environment variable is only read at process startup, not at runtime import.

**Impact:** No behavior changes; only diagnostic output suppression.
